name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Sema
        uses: sema-lisp/setup-sema@main

      - name: Run tests
        run: sema test.sema
